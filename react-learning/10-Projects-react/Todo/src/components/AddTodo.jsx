import React from 'react'

function AddTodo() {
    const handleAddTodo = () => {
        e.preventDefault();
        if (inputText)
    }
  return (
    <div>AddTodo</div>
  )
}

export default AddTodo